const code = `
        const list = [1, 2, 3, 4];

        function filterAndPrint(list) {
            list = list.filter((i, b) => a);

            list.forEach(i => console.log(i));

            return false;
        }

        filterAndPrint(list);
`;
