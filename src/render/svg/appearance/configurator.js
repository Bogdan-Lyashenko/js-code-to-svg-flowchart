//TODO: move initialisation logic from StyleTheme and TextContent here
//So it can keep config object and can be changed from outside
/*
*
* main.js
* JStoFlowChartWhatever.configureBorderColor({color: 'red'});
*
* //some abstractions layers like
 * getTheme = ()=> {
 *      return JStoFlowChartWhatever.getBorderColor();
 * }
 *
 *
 * theme = getTheme()
 * Rectangle(node, theme)
*
*
*
* */
